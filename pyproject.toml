[project]
name = "textbased-roguelite"
version = "0.1.0"
description = "Procedural text-driven roguelike game."
authors = [{ name = "AutoGenerated" }]
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
roguelite = "roguelite.cli:main"

[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"
